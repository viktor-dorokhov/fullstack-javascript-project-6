extends ../layouts/application.pug

block header
  = t('views.statuses.new.title')

block content
  +formFor()(action=route('statuses') method='post')
    +input(status, 'name', t('views.statuses.name'), errors, {autofocus: true})(type="text")
    +submit(status)(value=t('views.statuses.new.submit'))
    +back(t('views.statuses.new.back'), route('statuses'))
